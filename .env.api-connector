# ============================================
# API Connector Configuration for Azure Entra ID
# ============================================

# Authentication Credentials
API_CONNECTOR_USERNAME=your_api_connector_username
API_CONNECTOR_PASSWORD=your_secure_password

# API Connector Settings
API_CONNECTOR_ENDPOINT=https://your-server/api/v1/identity/resolve-claims

# Logging Configuration
API_CONNECTOR_LOG_LEVEL=info

# CORS Settings (if needed)
ALLOWED_ORIGINS=https://*.b2clogin.com

# Optional: Custom Claims Configuration
DEFAULT_CUSTOMER_TYPE=Individual
DEFAULT_AFFILIATION=Member

# ============================================
# Instructions:
# 1. Copy this file to .env.api-connector
# 2. Replace placeholder values with actual credentials
# 3. Ensure the endpoint URL matches your deployed server
# 4. Set up corresponding values in Azure Portal
# ============================================